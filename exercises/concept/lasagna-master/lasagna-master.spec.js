import { timeToMixJuice } from './lasagna-master';

FIXME

describe('timeToMixJuice', () => {
  test("returns the correct time for 'Pure Strawberry Joy'", () => {
    expect(timeToMixJuice('Pure Strawberry Joy')).toBe(0.5);
  });
});
