export class Person {
  /**
   * @param {Name} name
   * @param {Born} born
   * @param {Address} address
   */
  constructor(name, born, address) {
    this.name = name;
    this.born = born;
    this.address = address;
  }
}
